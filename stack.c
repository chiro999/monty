#include "monty.h"

/**
 * _stack - sets the data structure to a stack (LIFO)
 * @head: stack head
 * @counter: line_number
 *
 * Return: no return
 */
void _stack(stack_t **head, unsigned int counter)
{
    /* Suppress unused parameter warnings. */
    (void)head;
    (void)counter;

    /* Set the data structure to a stack (LIFO). */
    monty.lifi = 0;
}
