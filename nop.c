#include "monty.h"

/**
 * _nop - does nothing
 * @head: stack head
 * @counter: line_number
 *
 * Return: no return
 */
void _nop(stack_t **head, unsigned int counter)
{
    /* Suppress unused parameter warnings. */
    (void)counter;
    (void)head;
}
